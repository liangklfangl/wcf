import webpackDefaultConfig from './getWebpackDefaultConfig';

/**
 * @param  {[type]} 
 * @param  {Function} 
 * @return {[type]}
 */
export default function build(program,callback){

 var defaultWebpackConfig=webpackDefaultConfig(program);
 //get default webpack configuration

 

}